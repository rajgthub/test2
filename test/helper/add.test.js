const expect = require("expect");
describe("Valid revie", () => {
  it("should retrun true for valid review", () => {
    expect(3).toBe(3);
  });
  it("should retrun false for invalid review", () => {
    expect(3).toBe(3);
  });
});